<UserControl x:Class="PG.Control.Shape.PolygonMeshView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.Control.Shape"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:PolygonMeshViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <TextBlock Text="{Binding Id.Value}"/>
        <TextBlock Text="VertexCount"/>
        <TextBox Text="{Binding VertexCount.Value}"/>
        <TextBlock Text="FaceCount"/>
        <TextBox Text="{Binding FaceCount.Value}"/>
        <TextBlock Text="FaceGroupCount"/>
        <TextBox Text="{Binding FaceGroupCount.Value}"/>
    </StackPanel>
</UserControl>
