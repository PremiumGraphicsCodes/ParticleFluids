<UserControl x:Class="PG.Control.Graphics.ColorRGBAView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.Control.Graphics"
             mc:Ignorable="d" 
             d:DesignWidth="300">
    <UserControl.DataContext>
        <local:ColorRGBAViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Slider Value="{Binding R.Value}" Width="256"/>
            <TextBox Text="{Binding R.Value}" Width="50"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Slider Value="{Binding G.Value}" Width="256"/>
            <TextBox Text="{Binding G.Value}" Width="50"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Slider Value="{Binding B.Value}" Width="256"/>
            <TextBox Text="{Binding B.Value}" Width="50"/>
        </StackPanel>
        <Slider Value="{Binding A.Value}"/>
    </StackPanel>
</UserControl>
