<UserControl x:Class="PG.Control.Graphics.TextureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.Control.Graphics"
             mc:Ignorable="d" 
             d:DesignWidth="300">
    <UserControl.DataContext>
        <local:TextureViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Content="Select" Command="{Binding FileSelectCommand}"/>
            <TextBlock Text="Path"/>
            <TextBox Text="{Binding ImagePath.Value}" Width="100"/>
        </StackPanel>
        <Image Source="{Binding ImagePath.Value}"/>
    </StackPanel>
</UserControl>
