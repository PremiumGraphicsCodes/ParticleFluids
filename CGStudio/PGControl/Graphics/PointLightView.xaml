<UserControl x:Class="PG.Control.Graphics.PointLightView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.Control.Graphics"
             xmlns:control="clr-namespace:PG.Control"
             xmlns:math="clr-namespace:PG.Control.Math"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:PointLightViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <math:Vector3dView DataContext="{Binding Position}"/>
        <local:ColorRGBAView DataContext="{Binding Ambient}"/>
        <local:ColorRGBAView DataContext="{Binding Diffuse}"/>
        <local:ColorRGBAView DataContext="{Binding Specular}"/>
    </StackPanel>
</UserControl>
