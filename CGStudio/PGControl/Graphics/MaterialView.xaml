<UserControl x:Class="PG.Control.Graphics.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.Control.Graphics"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:MaterialViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="ID"/>
            <TextBlock Text="{Binding ID.Value}"></TextBlock>
        </StackPanel>
        <StackPanel>
            <TextBlock Text="Ambient"/>
            <local:ColorRGBAView DataContext="{Binding Ambient}"/>
        </StackPanel>
        <StackPanel>
            <TextBlock Text="Diffuse"/>
            <local:ColorRGBAView DataContext="{Binding Diffuse}"/>
        </StackPanel>
        <StackPanel>
            <TextBlock Text="Specular"/>
            <local:ColorRGBAView DataContext="{Binding Specular}"/>
        </StackPanel>
        <StackPanel>
            <TextBlock Text="Shininess"/>
            <TextBox Text="{Binding Shininess.Value}" Width="50"/>
        </StackPanel>
    </StackPanel>
</UserControl>
