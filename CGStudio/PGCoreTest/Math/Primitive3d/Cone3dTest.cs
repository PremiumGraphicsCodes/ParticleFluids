using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using PG.Core.Math;

namespace PGCoreTest.Math.Primitive3d
{
    [TestClass]
    public class Cone3dTest
    {
        /*
        [TestMethod]
        public void TestGetPosition()
        {
            var cone = new Cone3d(1.0, 1.0, new Vector3d(0,0,0));
            var actual = cone.GetPosition(0.0, 1.0, 1.0);
            var expected = new Vector3d(1, 0, 1);
        }
        */
    }
}
