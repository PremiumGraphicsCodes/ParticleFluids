<UserControl x:Class="FluidStudio.FileIO.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FluidStudio.FileIO"
             xmlns:prism="http://prismlibrary.com/" xmlns:ui="clr-namespace:PG.Control.UI;assembly=PGControl"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <StackPanel>
        <ui:SceneSelectView DataContext="{Binding SceneSelectViewModel}"/>
        <StackPanel Orientation="Horizontal">
            <Button Content="File" Command="{Binding FileSelectCommand}"/>
            <TextBox Text="{Binding FilePath.Value}" MinWidth="100"/>
        </StackPanel>
        <Button Content="Export" Command="{Binding ExportCommand}"/>
    </StackPanel>
</UserControl>
