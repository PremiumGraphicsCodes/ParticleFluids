<UserControl x:Class="FluidStudio.Scene.ParticleSystem.BoxGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FluidStudio.Scene.ParticleSystem"
             mc:Ignorable="d"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel>
        <math:Box3dView DataContext="{Binding BoxViewModel}"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Dx"/>
            <TextBox Text="{Binding Dx.Value}" MinWidth="50"/>
            <TextBox Text="{Binding Dy.Value}" MinWidth="50"/>
            <TextBox Text="{Binding Dz.Value}" MinWidth="50"/>
        </StackPanel>            
        <TextBox Text="{Binding Count.Value}"/>
        <Button Content="Generate" Command="{Binding GenerationCommand}"/>
    </StackPanel>
</UserControl>
