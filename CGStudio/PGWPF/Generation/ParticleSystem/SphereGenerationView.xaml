<UserControl xmlns:Control="clr-namespace:PG.Control;assembly=PGControl"  x:Class="PG.CGStudio.Generation.ParticleSystem.SphereGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Generation.ParticleSystem"
             xmlns:graphics="clr-namespace:PG.Control.Graphics;assembly=PGControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:SphereGenerationViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <Control:Sphere3dView DataContext="{Binding SphereViewModel}"/>
        <TextBox Text="{Binding Count.Value}"/>
        <StackPanel>
            <TextBlock Text="Color"/>
            <graphics:ColorRGBAView DataContext="{Binding ColorViewModel}"/>
        </StackPanel>
        <Button Content="Generate" Command="{Binding GenerationCommand}"/>
    </StackPanel>
</UserControl>
