<UserControl x:Class="PG.CGStudio.Generation.ParticleSystem.ParticleSystemGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Generation.ParticleSystem"
             xmlns:graphics="clr-namespace:PG.Control.Graphics;assembly=PGControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:BoxGenerationViewModel}">
            <local:BoxGenerationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:SphereGenerationViewModel}">
            <local:SphereGenerationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:CylinderGenerationViewModel}">
            <local:CylinderGenerationView/>
        </DataTemplate>
    </UserControl.Resources>
    <StackPanel>
        <ContentControl Content="{Binding ContentViewModel.Value}"/>
    </StackPanel>
</UserControl>
