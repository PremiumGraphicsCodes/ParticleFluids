<UserControl x:Class="PG.CGStudio.Selection.RegionSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Selection"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:RegionSelectViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Box"/>
            <local:BoxRegionSelectView DataContext="{Binding BoxRegionSelectViewModel}"/>
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Sphere"/>
            <local:SphereRegionSelectView DataContext="{Binding SphereRegionSelectViewModel}"/>
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Cylinder"/>
            <local:CylinderRegionSelectView DataContext="{Binding CylinderRegionSelectViewModel}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
