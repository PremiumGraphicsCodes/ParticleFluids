<UserControl x:Class="PG.CGStudio.Selection.ParticleSystemObjectSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Selection"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             xmlns:shape="clr-namespace:PG.Control.Shape;assembly=PGControl"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:ParticleSystemObjectSelectViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <Button Content="Pick" Command="{Binding PickCommand}"/>
        <shape:ParticleSystemView DataContext="{Binding ParticleSystemViewModel}"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Id"/>
            <TextBox Text="{Binding Id.Value}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="PointId"/>
            <TextBox Text="{Binding PointId.Value}"/>
        </StackPanel>
        <math:Vector3dView DataContext="{Binding PositionViewModel}"/>
    </StackPanel>
</UserControl>
