<UserControl x:Class="PG.CGStudio.Scene.Shape.WireFrame.Generation.TorusGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Scene.Shape.WireFrame.Generation"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             xmlns:wireframe="clr-namespace:PG.CGStudio.Generation.WireFrame"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <StackPanel>
        <math:Torus3dView DataContext="{Binding TorusViewModel}"/>
        <Label Content="UNum"/>
        <TextBox Text="{Binding UNum.Value}"/>
        <Label Content="VNum"/>
        <TextBox Text="{Binding VNum.Value}"/>
        <wireframe:AppearanceView DataContext="{Binding AppearanceViewModel}"/>
        <Button Content="Generate" Command="{Binding GenerationCommand}"/>
    </StackPanel>
</UserControl>
