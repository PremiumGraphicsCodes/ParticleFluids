<UserControl x:Class="PG.CGStudio.Generation.WireFrame.CircleGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Generation.WireFrame"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:CircleGenerationViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <math:Circle3dView DataContext="{Binding CircleViewModel}"/>
        <TextBox Text="{Binding UNum.Value}"/>
        <local:AppearanceView DataContext="{Binding AppearanceViewModel}"/>
        <Button Content="OK" Command="{Binding OkCommand}"/>
    </StackPanel>
</UserControl>
