<UserControl x:Class="PG.CGStudio.Generation.WireFrame.AppearanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Generation.WireFrame"
             xmlns:graphics="clr-namespace:PG.Control.Graphics;assembly=PGControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:AppearanceViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <graphics:ColorRGBAView DataContext="{Binding ColorViewModel}"/>
        <TextBox Text="{Binding Width.Value}" Width="50"/>
    </StackPanel>
</UserControl>
