<UserControl x:Class="PG.CGStudio.Scene.Shape.WireFrame.WireFrameEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Scene.Shape.WireFrame"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:WireFrameEditViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <TextBlock Text="Id"/>
        <TextBox Text="{Binding Id.Value}"/>
        <TextBlock Text="Name"/>
        <TextBox Text="{Binding Name.Value}"/>
        <Button Content="Edit" Command="{Binding EditCommand}"/>
    </StackPanel>
</UserControl>
