<UserControl x:Class="PG.CGStudio.Scene.Shape.Trim.TrimView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Scene.Shape.Trim"
             xmlns:select="clr-namespace:PG.CGStudio.Object.Select"
             xmlns:selection="clr-namespace:PG.CGStudio.Selection"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:TrimViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <select:ShapeSelectView DataContext="{Binding ShapeSelectViewModel}"/>
        <selection:SphereRegionSelectView DataContext="{Binding RegionSelectViewModel}"/>
        <Button Content="OK" Command="{Binding OkCommand}"/>
    </StackPanel>
</UserControl>
