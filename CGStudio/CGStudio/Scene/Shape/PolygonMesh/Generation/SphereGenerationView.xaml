<UserControl xmlns:Control="clr-namespace:PG.Control;assembly=PGControl"
             x:Class="PG.CGStudio.Generation.PolygonMesh.SphereGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Generation.PolygonMesh"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <StackPanel>
        <Control:Sphere3dView DataContext="{Binding SphereViewModel}"/>
        <Label Content="UNum"/>
        <TextBox Text="{Binding UNum.Value}"/>
        <Label Content="VNum"/>
        <TextBox Text="{Binding VNum.Value}"/>
        <Button Content="Generate" Command="{Binding GenerateCommand}"/>
    </StackPanel>
</UserControl>
