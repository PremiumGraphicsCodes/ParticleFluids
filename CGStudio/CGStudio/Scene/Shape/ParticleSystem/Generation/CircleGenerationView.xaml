<UserControl x:Class="PG.CGStudio.Scene.Shape.ParticleSystem.Generation.CircleGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Scene.Shape.ParticleSystem.Generation"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             xmlns:ps="clr-namespace:PG.CGStudio.Generation.ParticleSystem"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:CircleGenerationViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <math:Circle3dView DataContext="{Binding CircleViewModel}"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Count"/>
            <TextBox Text="{Binding Count.Value}" MinWidth="50"/>
        </StackPanel>
        <ps:AppearanceView DataContext="{Binding AppearanceViewModel}"/>
        <Button Content="Generation" Command="{Binding GenerationCommand}"/>
    </StackPanel>
</UserControl>
