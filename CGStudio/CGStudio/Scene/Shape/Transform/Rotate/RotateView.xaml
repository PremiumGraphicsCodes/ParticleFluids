<UserControl x:Class="PG.CGStudio.Scene.Shape.Transform.Rotate.RotateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Scene.Shape.Transform.Rotate"
             xmlns:select="clr-namespace:PG.CGStudio.Object.Select"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             xmlns:transform="clr-namespace:PG.CGStudio.Scene.Shape.Transform"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <transform:RotateViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <select:SceneSelectView DataContext="{Binding ShapeSelectViewModel}"/>
        <math:Vector3dView DataContext="{Binding AngleViewModel}"/>
        <Button Content="Rotate" Command="{Binding RotateCommand}"/>
        <Button Content="OK" Command="{Binding OkCommand}"/>
    </StackPanel>
</UserControl>
