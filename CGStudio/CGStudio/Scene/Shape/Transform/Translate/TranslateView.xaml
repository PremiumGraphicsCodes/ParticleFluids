<UserControl x:Class="PG.CGStudio.Scene.Shape.Transform.TranslateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Scene.Shape.Transform"
             xmlns:ui="clr-namespace:PG.Control.UI;assembly=PGControl"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             xmlns:select="clr-namespace:PG.CGStudio.Object.Select"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:TranslateViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <TextBlock Text="{Binding ShapeId.Value}"/>
        <Button Content="OK" Command="{Binding OkCommand}"/>
        <Button Content="Cancel" Command="{Binding CancelCommand}"/>
    </StackPanel>
</UserControl>
