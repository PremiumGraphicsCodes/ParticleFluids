<UserControl x:Class="PG.CGStudio.Scene.SceneExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PG.CGStudio.Scene"
             xmlns:select="clr-namespace:PG.CGStudio.Object.Select"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:SceneExportViewModel/>
    </UserControl.DataContext>
    <StackPanel>
        <select:SceneSelectView DataContext="{Binding ShapeSelectViewModel}"/>
        <StackPanel Orientation="Horizontal">
           <TextBox Text="{Binding FilePath.Value}"/>
           <Button Content="Select"/>
        </StackPanel>
        <Button Content="Export" Command="{Binding ExportCommand}"/>
    </StackPanel>
</UserControl>
