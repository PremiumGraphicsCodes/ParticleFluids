<Window x:Class="FluidStudio.ProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FluidStudio"
        mc:Ignorable="d"
        Title="ProgressView" Height="200" Width="400">
    <Window.DataContext>
        <local:ProgressViewModel/>
    </Window.DataContext>
    <ProgressBar Width="200" Height="20" Minimum="{Binding Min.Value}" Maximum="{Binding Max.Value}" Value="{Binding Value.Value}"/>
</Window>
