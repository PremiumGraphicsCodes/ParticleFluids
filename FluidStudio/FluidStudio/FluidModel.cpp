#include "FluidModel.h"

#include "CrystalPhysics/CrystalPhysicsCommand/FluidSceneUpdateCommand.h"

using namespace Crystal::Scene;
using namespace Crystal::Physics;
using namespace Crystal::UI;

void FluidModel::send(World* world)
{
	/*
	FluidSceneUpdateCommand::Args args;
	args.particleSystemId.setValue(particleSystemIdView.getValue());
	args.stiffness.setValue(pressureCoeView.getValue());
	args.viscosity.setValue(viscosityCoeView.getValue());
	args.heatDiffuseCoe.setValue(heatDiffuseCoeView.getValue());
	args.dragForceCoe.setValue(dragForceCoeView.getValue());
	args.dragHeatCoe.setValue(dragHeatCoeView.getValue());
	FluidSceneUpdateCommand command(args);
	command.execute(getWorld());
	*/
}