<UserControl x:Class="FluidStudio.Scene.CSG.BoxGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FluidStudio.Scene.CSG"
             xmlns:math="clr-namespace:PG.Control.Math;assembly=PGControl"
             xmlns:graphics="clr-namespace:PG.Control.Graphics;assembly=PGControl"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <StackPanel>
        <math:Box3dView DataContext="{Binding BoxViewModel}"/>
        <graphics:ColorRGBAView DataContext="{Binding ColorViewModel}"/>
        <TextBox Text="{Binding Name.Value}"/>
        <Button Content="Generate" Command="{Binding GenerationCommand}"/>
    </StackPanel>
</UserControl>
