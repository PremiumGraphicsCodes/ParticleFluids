<UserControl x:Class="FluidStudio.Scene.Voxel.SphereGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FluidStudio.Scene.Voxel"
             xmlns:control="clr-namespace:PG.Control;assembly=PGControl"
             xmlns:prism="http://prismlibrary.com/"            
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <StackPanel>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Resolution">
                <TextBox Text="{Binding ResolutionX.Value}" MinWidth="50"/>
                <TextBox Text="{Binding ResolutionY.Value}" MinWidth="50"/>
                <TextBox Text="{Binding ResolutionZ.Value}" MinWidth="50"/>
            </TextBlock>
        </StackPanel>
        <control:Sphere3dView DataContext="{Binding SphereViewModel}"/>
        <Button Content="Generate" Command="{Binding GenerationCommand}"/>
    </StackPanel>
</UserControl>
